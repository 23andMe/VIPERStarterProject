# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'VIPERStarterProject' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for VIPERStarterProject
  pod 'Swinject'
  pod 'SwinjectPropertyLoader', '>= 1.0.0'

  target 'VIPERStarterProjectTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'VIPERStarterProjectUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
